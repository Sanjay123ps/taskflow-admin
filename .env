# Base URL of the backend API (see /docs or the backend repo once built).
# The Express/Fastify backend from the spec should expose everything under this prefix.
VITE_API_URL=http://localhost:4000/api/v1